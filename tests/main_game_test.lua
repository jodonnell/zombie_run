module(..., package.seeall)

require "main_game"

function test_classWorks()
   -- main_game = MainGame()

   -- local x, y = main_game:getSprites()[1]:getLoc()
   -- main_game:mainGameLoop()
   -- local after_x, after_y = main_game:getSprites()[1]:getLoc()
   -- assert_equal(y + 10, after_y)
end
